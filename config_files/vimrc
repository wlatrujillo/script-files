" Vim configuration file "                                                                                                                                                                                                              

" onable mouse support "
" set mouse=a

" enable sytax "
syntax on

" enable relative line numbers on "
set relativenumber number


" Automcaticaly wrap text that extends beyond the screen length "
set wrap

" Encoding "
set encoding=utf-8

" highlight current line "
set cursorline
:highlight Cursorline cterm=bold ctermbg=black

" enable highlight search pattern "
set incsearch hlsearch

" enable smartcase search sensitivity "
set ignorecase
set smartcase

" show the matching part of paris [] {} and () "
set showmatch

" show status bar 0, never 1 only if more than 2 windows, 2 always"
set laststatus=1

let mapleader=" "

inoremap jj <Esc>

nmap <Leader>w :w<CR>
nmap <Leader>q :q<CR>
nmap <Leader>x :x<CR>
